import noImage from "@/assets/no_image.png";
import bannerNoImage from "@/assets/banner_no_image.jfif";

/** Gambar pengganti untuk produk & kategori (kosong / gagal dimuat). */
export const NO_IMAGE = noImage;

/** Gambar pengganti untuk banner (kosong / gagal dimuat). */
export const BANNER_NO_IMAGE = bannerNoImage;
